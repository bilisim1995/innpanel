rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Gecici: tum dokumanlara tam erisim
    match /{document=**} {
      allow read, write: if true;
    }
  }
}